pipeline {
    agent any 
    stages {
        stage('Build') { 
            steps {
                // run script to pull users
                // convert mardown file to pdf
            }
        }
        stage('Test') { 
            steps {
                // Perform tests eg: does file exist?
            }
        }
        stage('Deploy') { 
            steps {
                // Start server
            }
        }
    }
}